
{% extends "layout.html" %}

{% block content %}
    <h3 class="text-center">{{saludos}}</h1>

    <div class="">
            <table class="table table-striped table-bordered table-condensed table-hover">
                <thead>
                    <th>Nombre</th>
                    <th>Código</th>
                    <th>&nbsp;</th>
                </thead>
                <tbody>
                    {% for row in paises %}
                        <tr>
                            <td>{{row.nombre}}</td>
                            <td>{{row.codigo}}</td>
                            <td><a href="{{path('registrar')}}" class="btn btn-default">Izquierdo</a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
    </div>
{% endblock %}
